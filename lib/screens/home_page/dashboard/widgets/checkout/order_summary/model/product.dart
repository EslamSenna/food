class Product {
  final String? img, title, subtitle, price;
  Product({ this.img, this.title, this.subtitle, this.price });
}

class Checkout{
  final String? title , subtitle;
  final style;

  Checkout({this.title, this.subtitle, this.style});
}